# 🏗️ System Architecture - Zyfar Pulse Survey System

This document provides visual and detailed architecture overview of the Zyfar Pulse Survey System.

---

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              React Frontend (Port 3000)                    │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │  │
│  │  │  Header  │  │ Category │  │  Survey  │  │ Insights │ │  │
│  │  │          │  │ Selector │  │ Section  │  │   Live   │ │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │  │
│  │  ┌──────────┐  ┌──────────┐                              │  │
│  │  │   City   │  │Community │                              │  │
│  │  │  Pulse   │  │  Share   │                              │  │
│  │  └──────────┘  └──────────┘                              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                           │                                      │
│                           │ HTTP/HTTPS Requests                  │
│                           ▼                                      │
└─────────────────────────────────────────────────────────────────┘
                            │
                            │
┌─────────────────────────────────────────────────────────────────┐
│                    UBUNTU VPS (Hostinger)                        │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  Nginx (Port 80/443)                       │  │
│  │  ┌─────────────────────┐  ┌──────────────────────────┐   │  │
│  │  │  Static Files       │  │  Reverse Proxy           │   │  │
│  │  │  (Frontend Build)   │  │  /api → localhost:4000   │   │  │
│  │  │  /var/www/...       │  │                          │   │  │
│  │  └─────────────────────┘  └──────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                      │                           │
│                                      │                           │
│  ┌──────────────────────────────────▼───────────────────────┐  │
│  │            Node.js + Express (Port 4000)                  │  │
│  │            Managed by PM2                                 │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │  │
│  │  │   Routes     │  │  Analytics   │  │  Data Store  │   │  │
│  │  │   - Submit   │  │  - Live Stats│  │  - Read JSON │   │  │
│  │  │   - Stats    │  │  - City View │  │  - Write JSON│   │  │
│  │  │   - Questions│  │  - Aggregation│  │             │   │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                      │                           │
│                                      │                           │
│  ┌──────────────────────────────────▼───────────────────────┐  │
│  │            JSON File Storage                              │  │
│  │        /var/zyfar_pulse/data/surveys.json                │  │
│  │                                                            │  │
│  │  [                                                         │  │
│  │    {                                                       │  │
│  │      "id": "survey_123...",                               │  │
│  │      "category": "travel_intention",                      │  │
│  │      "answers": [...],                                    │  │
│  │      "city": "Delhi",                                     │  │
│  │      "timestamp": "2025-11-26T..."                        │  │
│  │    }                                                       │  │
│  │  ]                                                         │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

### Survey Submission Flow

```
User Opens App
     │
     ▼
┌─────────────────────┐
│  Header Component   │ → User clicks "Start Survey Now"
└─────────────────────┘
     │
     ▼
┌─────────────────────┐
│ Category Selector   │ → User selects category (e.g., "Travel Intention")
└─────────────────────┘
     │
     ▼
┌─────────────────────┐
│  Survey Section     │ → User answers questions one by one
│  - Question 1       │    with progress tracking
│  - Question 2       │
│  - Question 3       │
│  - City Input       │
└─────────────────────┘
     │
     ▼
┌─────────────────────┐
│ POST /api/surveys/  │
│       submit        │ → { category, answers, city }
└─────────────────────┘
     │
     ▼
┌─────────────────────┐
│  Backend Server     │ → Validates data
│  - Validation       │ → Generates ID & timestamp
│  - Rate Limiting    │ → Appends to surveys.json
└─────────────────────┘
     │
     ▼
┌─────────────────────┐
│  surveys.json       │ → Data persisted
│  [{ survey data }]  │
└─────────────────────┘
     │
     ▼
┌─────────────────────┐
│  Response           │ → { success: true, insight: "..." }
└─────────────────────┘
     │
     ▼
┌─────────────────────┐
│  Live Insights      │ → Fetches GET /api/stats/live
│  - Pie Chart        │ → Displays personalized message
│  - Trends           │ → Shows visualizations
└─────────────────────┘
     │
     ▼
┌─────────────────────┐
│  City Pulse         │ → Fetches GET /api/stats/city-overview
│  - Safety Index     │ → Displays city-level stats
│  - Budget Average   │
└─────────────────────┘
```

---

## 🗂️ Component Architecture

### Frontend Component Hierarchy

```
App.tsx
│
├── Header
│   ├── Title
│   ├── Subtitle
│   ├── PulseAnimation
│   └── CTAButton
│
├── CategorySelector
│   └── CategoryCard (x10)
│       ├── Emoji
│       ├── Title
│       ├── Tagline
│       └── QuestionCount
│
├── SurveySection (conditional)
│   ├── CategoryHeader
│   ├── ProgressDots
│   ├── QuestionContainer
│   │   ├── QuestionText
│   │   └── OptionButtons
│   ├── CityInput
│   └── NavigationButtons
│
├── LiveInsights (conditional)
│   ├── InsightHighlight
│   ├── PieChart (Recharts)
│   ├── TrendLineChart (Recharts)
│   └── DestinationsList
│
├── CityPulseOverview
│   ├── TabButtons (x3)
│   ├── StatCards (x4)
│   │   ├── Safety Index
│   │   ├── Budget Average
│   │   ├── Happiness Score
│   │   └── Pain Point
│   └── DestinationCards
│
└── CommunityShare
    ├── SectionTitle
    ├── ShareButtons
    └── Footer
```

---

## 🔌 Backend API Architecture

### Route Structure

```
server.js
│
├── Middleware
│   ├── helmet() → Security headers
│   ├── cors() → Cross-origin requests
│   ├── express.json() → Body parsing
│   └── rateLimit() → Rate limiting
│
├── Routes
│   ├── GET /health
│   │   └── Returns: { status, timestamp, dataStore }
│   │
│   ├── POST /api/surveys/submit
│   │   ├── Validate: category, answers
│   │   ├── Call: dataStore.addSurvey()
│   │   ├── Calculate: personalized insight
│   │   └── Returns: { success, id, insight }
│   │
│   ├── GET /api/stats/live?category=X
│   │   ├── Call: calculateLiveStats()
│   │   ├── Aggregate: survey responses
│   │   └── Returns: { totalResponses, aggregated, trends }
│   │
│   ├── GET /api/stats/city-overview?city=X
│   │   ├── Call: calculateCityOverview()
│   │   ├── Calculate: safety, budget, happiness
│   │   └── Returns: { city, indexes, destinations }
│   │
│   └── GET /api/surveys/sample-questions?category=X
│       ├── Read: surveyConfig.js
│       └── Returns: { categories or specific category }
│
└── Error Handlers
    ├── 404 Handler
    └── 500 Handler
```

---

## 💾 Data Storage Architecture

### File System Structure

```
/var/zyfar_pulse/
└── data/
    └── surveys.json
        │
        └── [
              {
                "id": "survey_1732617000000_abc123",
                "category": "travel_intention",
                "questions": ["ti_q1", "ti_q2", "ti_q3"],
                "answers": [
                  "Yes, definitely",
                  "Manali",
                  "Solo adventure"
                ],
                "city": "Delhi",
                "timestamp": "2025-11-26T10:30:00.000Z"
              },
              {
                "id": "survey_1732617100000_xyz789",
                "category": "safety",
                "questions": ["sf_q1", "sf_q2", "sf_q3"],
                "answers": [
                  "Very safe",
                  "No concerns",
                  "Yes, absolutely"
                ],
                "city": "Mumbai",
                "timestamp": "2025-11-26T10:31:40.000Z"
              }
            ]
```

### Data Operations

```
DataStore Operations
│
├── initialize()
│   ├── Create /var/zyfar_pulse/data/ if not exists
│   └── Create surveys.json if not exists
│
├── readSurveys()
│   ├── Read surveys.json
│   ├── Parse JSON
│   └── Return array
│
├── writeSurveys(surveys)
│   ├── Stringify surveys array
│   ├── Write to surveys.json
│   └── Return success/failure
│
├── addSurvey(surveyData)
│   ├── Read existing surveys
│   ├── Generate ID and timestamp
│   ├── Push new survey
│   ├── Write back to file
│   └── Return { success, id }
│
├── getSurveysByCategory(category)
│   ├── Read all surveys
│   └── Filter by category
│
├── getSurveysByCity(city)
│   ├── Read all surveys
│   └── Filter by city
│
└── getStats()
    ├── Read all surveys
    ├── Calculate category breakdown
    ├── Calculate city breakdown
    └── Return statistics
```

---

## 🔐 Security Architecture

### Security Layers

```
┌─────────────────────────────────────────────────┐
│              Security Layer 1: Network           │
│  - HTTPS/SSL (Certbot)                          │
│  - Firewall (UFW)                               │
│  - Nginx security headers                       │
└─────────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────┐
│           Security Layer 2: Application          │
│  - Helmet.js (security headers)                 │
│  - CORS configuration                           │
│  - Rate limiting (express-rate-limit)           │
│    • 100 requests / 15 min (general)            │
│    • 5 submissions / 1 min (surveys)            │
└─────────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────┐
│            Security Layer 3: Input               │
│  - Category validation                          │
│  - Answers validation                           │
│  - Type checking (TypeScript)                   │
│  - Body size limits                             │
└─────────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────┐
│           Security Layer 4: Storage              │
│  - No SQL injection (JSON storage)              │
│  - File permissions                             │
│  - Directory isolation                          │
└─────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

### Production Environment

```
Internet
   │
   ▼
[Domain: yourdomain.com] → DNS → [VPS IP Address]
   │
   ▼
┌──────────────────────────────────────────────────┐
│              Ubuntu VPS (Hostinger)               │
│                                                    │
│  ┌────────────────────────────────────────────┐  │
│  │           Nginx (Web Server)               │  │
│  │  Port 80 → Redirect to 443                 │  │
│  │  Port 443 → SSL/HTTPS                      │  │
│  │                                             │  │
│  │  Location /     → Frontend Static Files    │  │
│  │  Location /api  → Proxy to localhost:4000  │  │
│  └────────────────────────────────────────────┘  │
│                          │                         │
│                          ▼                         │
│  ┌────────────────────────────────────────────┐  │
│  │          PM2 Process Manager               │  │
│  │  ┌──────────────────────────────────────┐ │  │
│  │  │  App: zyfar-pulse                    │ │  │
│  │  │  Script: server.js                   │ │  │
│  │  │  Instances: 1                        │ │  │
│  │  │  Status: online                      │ │  │
│  │  │  Auto-restart: enabled               │ │  │
│  │  └──────────────────────────────────────┘ │  │
│  └────────────────────────────────────────────┘  │
│                          │                         │
│                          ▼                         │
│  ┌────────────────────────────────────────────┐  │
│  │      Node.js Application (Port 4000)       │  │
│  │      - Express Server                      │  │
│  │      - API Routes                          │  │
│  │      - Data Store                          │  │
│  └────────────────────────────────────────────┘  │
│                          │                         │
│                          ▼                         │
│  ┌────────────────────────────────────────────┐  │
│  │          File System Storage               │  │
│  │  /var/zyfar_pulse/data/surveys.json       │  │
│  └────────────────────────────────────────────┘  │
│                                                    │
│  ┌────────────────────────────────────────────┐  │
│  │          SSL Certificates                  │  │
│  │  /etc/letsencrypt/live/yourdomain.com/    │  │
│  │  - fullchain.pem                           │  │
│  │  - privkey.pem                             │  │
│  │  Auto-renewal: Certbot cron job           │  │
│  └────────────────────────────────────────────┘  │
│                                                    │
└──────────────────────────────────────────────────┘
```

---

## 📈 Analytics Architecture

### Statistics Calculation Flow

```
Survey Data → Analytics Engine → Insights
     │              │                │
     │              │                ▼
     │              │         ┌──────────────┐
     │              │         │ Live Stats   │
     │              │         │ - Pie Chart  │
     │              │         │ - Trends     │
     │              │         │ - Top Dest   │
     │              │         └──────────────┘
     │              │
     │              ▼
     │       ┌─────────────────────┐
     │       │ Aggregation Logic   │
     │       │ - Group by question │
     │       │ - Count answers     │
     │       │ - Calculate %       │
     │       └─────────────────────┘
     │              │
     ▼              ▼
┌──────────────────────────────┐
│  calculateLiveStats()         │
│  - Filter by category         │
│  - Aggregate answers          │
│  - Extract destinations       │
│  - Generate trends (30 days)  │
└──────────────────────────────┘
     │
     ▼
┌──────────────────────────────┐
│  calculateCityOverview()      │
│  - Filter by city             │
│  - Calculate safety index     │
│  - Calculate budget average   │
│  - Calculate happiness score  │
│  - Identify pain points       │
└──────────────────────────────┘
```

---

## 🔧 Technology Stack Diagram

```
┌─────────────────────────────────────────────────────────┐
│                     Frontend Stack                       │
├─────────────────────────────────────────────────────────┤
│  React 18           │ UI Library                         │
│  TypeScript         │ Type Safety                        │
│  Vite              │ Build Tool & Dev Server            │
│  Recharts          │ Data Visualization                 │
│  Lucide React      │ Icons                              │
│  SCSS/CSS Modules  │ Styling                            │
│  Poppins           │ Font                               │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                     Backend Stack                        │
├─────────────────────────────────────────────────────────┤
│  Node.js 18+       │ Runtime Environment                │
│  Express           │ Web Framework                      │
│  Helmet            │ Security Headers                   │
│  CORS              │ Cross-Origin Requests              │
│  express-rate-limit│ Rate Limiting                      │
│  PM2               │ Process Management                 │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                 Infrastructure Stack                     │
├─────────────────────────────────────────────────────────┤
│  Ubuntu 20.04+     │ Operating System                   │
│  Nginx             │ Web Server & Reverse Proxy         │
│  Certbot           │ SSL Certificates                   │
│  JSON Files        │ Data Storage                       │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 Request/Response Flow

### Complete HTTP Flow

```
[1] User Action
    │
    ▼
[2] React Component
    │
    ▼
[3] API Service Layer (frontend/src/services/api.ts)
    │
    ▼
[4] HTTP Request
    │ Method: POST
    │ URL: /api/surveys/submit
    │ Headers: { Content-Type: application/json }
    │ Body: { category, answers, city }
    │
    ▼
[5] Nginx (Port 443)
    │ SSL Termination
    │ Proxy to localhost:4000
    │
    ▼
[6] Express Middleware Chain
    │ → helmet() (security)
    │ → cors() (CORS)
    │ → express.json() (parsing)
    │ → rateLimit() (throttling)
    │
    ▼
[7] Route Handler (backend/server.js)
    │ Validation
    │ Business Logic
    │
    ▼
[8] Data Store (backend/utils/dataStore.js)
    │ Read surveys.json
    │ Append new survey
    │ Write surveys.json
    │
    ▼
[9] Response
    │ Status: 200
    │ Body: { success: true, id: "...", insight: "..." }
    │
    ▼
[10] Frontend Receives Response
    │ Update state
    │ Show insights
    │ Render charts
```

---

## 📊 Monitoring & Logging

```
┌─────────────────────────────────────────┐
│          Application Logs                │
├─────────────────────────────────────────┤
│  PM2 Logs                               │
│  - /var/log/zyfar-pulse-out.log        │
│  - /var/log/zyfar-pulse-error.log      │
│  Commands: pm2 logs zyfar-pulse        │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│           Web Server Logs                │
├─────────────────────────────────────────┤
│  Nginx Access Log                       │
│  - /var/log/nginx/access.log           │
│                                          │
│  Nginx Error Log                        │
│  - /var/log/nginx/error.log            │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         System Monitoring                │
├─────────────────────────────────────────┤
│  PM2 Status: pm2 status                │
│  Process Health: pm2 monit             │
│  Disk Usage: df -h                     │
│  Memory: free -h                       │
└─────────────────────────────────────────┘
```

---

## 🔄 Update & Maintenance Flow

```
[Code Update]
     │
     ▼
[Local Development]
     │ Test thoroughly
     ▼
[Build Frontend]
     │ npm run build
     ▼
[Upload to VPS]
     │ SCP or Git
     ▼
[Backend Update]
     │ npm install (if deps changed)
     │ pm2 restart zyfar-pulse
     ▼
[Frontend Update]
     │ Replace /var/www/.../dist/
     │ sudo systemctl reload nginx
     ▼
[Verification]
     │ Check health endpoint
     │ Test survey submission
     │ Verify charts render
     ▼
[Monitor Logs]
     │ pm2 logs zyfar-pulse
     │ sudo tail -f /var/log/nginx/error.log
```

---

This architecture is designed for:
- ✅ **Simplicity** - Easy to understand and maintain
- ✅ **Scalability** - Can handle thousands of surveys
- ✅ **Reliability** - PM2 auto-restart, Nginx stability
- ✅ **Security** - Multiple security layers
- ✅ **Performance** - Fast JSON operations, efficient caching
- ✅ **Maintainability** - Clear separation of concerns

For implementation details, refer to the source code and documentation files.
